# Agent.md — videofp 项目交接说明

> 交接时间：2026-09-16 22:00（CST）
> 交接对象：接管本项目的编程 Agent
> 交接人声明：本文件来自一次人机对话的整理。**所有无法用文件或输出证明的内容，均标注了证据等级或列入「待定」；请勿把「未验证」和「待定」当作结论使用。**

---

## ⚠️ 0. 接手 Agent 的修订说明（请先读这一节）

> 修订时间：本次接手会话 · 修订人：接手 Agent
> 修订范围：**仅环境与仓库现状（§3）及随之失效的引用**。项目目标、硬约束、
> 评测协议、待办优先级**一律未改动**。

本次接手发现三个必须先说清楚的事实，否则后面所有结论都会建立在错的前提上：

### 0.1 本文件的机器配置已经过时（原 §3.1 已重写）

原文 §3.1 记录的是**另一台机器**（Linux / Pentium G4560 / 2 核 4 线程 / 无 GPU /
`/AstrBot/data`）。**当前这台机器是完全不同的硬件和操作系统**，且整体快一个
数量级。凡原文中依赖机器能力的判断（尤其 §2 C4「当前机器无 GPU」、
§1.3「不对全量视频库做语义嵌入」的理由、§11 的各项估算），**请以下面
重写后的 §3.1 为准重新评估**。

### 0.2 本仓库此前**不含任何代码**

接手时本目录只有三个文件：`Agent.md`（25 625 字节）、以及**均为 0 字节**的
`README.md` 与 `CHANGELOG.md`。原文 §3.2 所述的 `videofp/` 仓库、§3.4 的
15 个文件、§4 的两套语料、`../research/` 一期素材，**在本机全部不存在**。
原文的 git 4 个提交（`66a2701`…`e9b2c1e`）也不存在——本目录当时尚未初始化仓库。

因此：**原文 §5「已验证的事实」、§6「未完成清单」、§7「待办」中针对具体文件的
描述，在本机无法直接核对。** 本次接手按原文的**目标、约束与评测协议**重新
实现了一套可运行的管线，写在 `vfp/` + `tools/` + `tests/` + `demo.py`，
并在 §14 逐条列出「原文声明 vs 本次实测」的对照，明确区分：
哪些是原文已有结论被本机复现、哪些是本机新测、哪些仍未知。

### 0.3 原文中被证伪/需修正的技术点

本次实现在编码与测试中定位到原文若干技术描述的问题，已在新实现里修正，
并在 §14.3 详列。最需要注意的是：

1. **镜像双查询不能靠哈希位重排实现**。原文 §9 只说"翻转哈希参与比对"，
   未说怎么翻转。实测证明：靠 64 bit 位重排是**数学上不成立**的
   （镜像会让 DCT 奇数列系数翻转符号，位序与判定同时改变，不存在纯位置换解），
   必须**在像素域把帧翻转后重新算哈希**。
2. **倍速估计存在可辨识性问题**。原文把"倍速估计"当作已解决的能力（§5.1 记录了
   1.05 恢复）。实测：短查询（如 12 帧/6 秒）下 1.0 与 1.05 命中数并列，
   **倍速在信息论意义上不可辨识**；此时强行输出精修倍速会把时间码算错
   （6 秒素材被报成 5 秒）。新实现增加了可辨识性判据，不可辨识时退回网格值。
3. **原 §2 C6 的依赖约束需放宽**：原实现要求"仅 numpy + Pillow，不用 scipy"。
   实测自研正交 DCT 与 `imagehash` 使用的 `scipy.fftpack.dct`（未归一化）**不是
   同一个尺度**，会让 4 张测试图共 **112 bit** 判定不一致；改用 scipy 后差异为
   **0 bit**。`scipy` 本就是 `imagehash` 的传递依赖，不新增实际负担。
   详见 §14.3。

---

## 0. 证据分级约定（先读这一节，全文档按此标注）

| 标记 | 含义 | 使用纪律 |
|---|---|---|
| **【已实测】** | 在本机实际执行并产生了可查看的输出（终端输出 / JSON / CSV / 测试通过） | 可作为事实引用，但必须同时引用其**口径**（样本数、采样率、阈值） |
| **【已实测·样本极小】** | 跑过，但样本量不足以支撑统计结论 | 只能当方向性信号，不能写进论文当结论 |
| **【未验证】** | 代码/脚本已写、语法通过，但**从未执行过** | 禁止当作已完成；使用前必须先跑 |
| **【引用】** | 来自公开资料/官方基准，非本项目产出 | 引用时须注明来源 |
| **【待定】** | 我确实拿不准，已明确留空 | **禁止脑补填充**；需要时先做实验再定 |
| **【估算】** | 由硬件倍率推算，无实测 | 禁止写进论文当实验结果 |

**两条全局纪律**：
1. **口径不同的数字不可跨文档比较。** 本仓库存在两套互不兼容的评测管线（见 §4），混用会造成自相矛盾。
2. **禁止用"看视频觉得像"做任何判断。** 这是本项目的硬约束（§2 C1）。所有结论必须来自算法输出。

---

## 1. 项目目标

### 1.1 主目标（毕设核心，必须先闭环）

构建一套**视频盗版/抄袭鉴定系统**，输入一个疑似盗版视频与一个参考片库，输出：
- 是否命中（同一性判定）
- 命中哪个参考片
- **命中的时间码区间**（片段级定位，可作证据）
- 判定依据（匹配帧数、覆盖率、平均 Hamming 距离、疑似倍速系数）

技术路线：**感知指纹（pHash/其他）+ 几何归一化 + 序列对齐与片段定位 + 音频指纹 + 自动评测**。

### 1.2 增值目标（毕设"锦上添花"，可选，不阻塞主目标）

**语义检索层**：用自然语言搜视频片段（"搜'枪战'跳转到第几分钟"），定位为**发现层/召回层**。
已论证的架构为三层：
- L1 语义召回（多模态嵌入 + ANN）：把库缩小到千分之一
- L2 指纹精判（pHash + 序列对齐）：判定同一性并出时间码证据
- L3 语义聚类：发现"同一素材被多部作品复用"（换皮/重拍）

### 1.3 非目标（明确不做，避免跑偏）

- ❌ 不对全量视频库做语义嵌入（成本不成立，理由见 `docs/语义检索硬件要求评估.md`）
- ❌ 不训练任何模型（只用现成编码器，且需先通过 §2 C4）
- ❌ 不把语义相似度当侵权证据（它只能当线索）
- ❌ 不实现云端服务/多租户

---

## 2. 硬约束（不可协商）

| 编号 | 约束 | 原因 / 来源 |
|---|---|---|
| **C1** | **禁止用视觉能力（人眼或模型"看图判断"）判断两个视频是否为同一份**；一切判定只能来自算法输出数字 | 用户明确要求；保证评测客观性 |
| **C2** | **评测不得泄漏标签**：待检文件名必须中性化（`q001.mp4`），真实含义只写在 `groundtruth.csv`；**匹配算法只接收文件路径，永不读标签** | 防止"自己污染自己"的评测 |
| **C3** | **必须包含负样本**（与所有参考片无关的视频），否则假阳率无法计算 | 评测有效性 |
| **C4** | ~~当前机器**无 GPU**~~ → **已修正（见 §3.1.1）**：本机有 AMD Radeon RX 590 GME（4 GiB VRAM），但**不是 NVIDIA、无 CUDA**，`torch`/`cuda` 仍未安装。原约束的**实质**——不引入 torch/cuda 等重依赖，除非用户明确批准并接受下载时长——**继续有效** | 本机条件（§3.1） |
| **C5** | 网络需经本机代理 `127.0.0.1:3067`；直连超时。避免大文件下载（>100MB 需先请示）。**注**：本次已按用户明确指示下载 ffmpeg（161 MB） | 本机条件（§3.1） |
| **C6** | 依赖保持轻量：`numpy` + `Pillow` + `scipy.fftpack` + `ffmpeg/ffprobe`。**已修正**：原写"不用 scipy"，但自研 DCT 与 imagehash 位级不一致（112 bit 差异），且 scipy 本就是 imagehash 的传递依赖，故纳入。`imagehash` 仅用于**测试对照**，不进入主流程 | 可离线、可移植 |
| **C7** | 输出必须可复现：同一输入 → 同一数字。涉及随机的地方必须固定 seed | 论文可复现性 |
| **C8** | 不得在测试集上反复调阈值后宣称性能；阈值调参须有明确的调参集 | 防自欺 |

---

## 3. 环境与仓库现状

### 3.1 本机环境

#### 3.1.1 当前机器【已实测 本次接手会话】

由 `Get-CimInstance` / `[System.Environment]` 直接读取，非推算。

| 项 | 值 |
|---|---|
| OS | **Windows 10 IoT 企业版 LTSC**，10.0.19044（64 位） |
| Shell | **PowerShell 7.6.6**（`pwsh`），非 POSIX |
| CPU | **Intel Core i5-12400F**，**6 核 12 线程**，2.5 GHz 基准 |
| 内存 | **31.8 GiB**（2×16 GiB DDR4-3600 SK Hynix；实测可用 7.3 GiB，被其它进程占用） |
| GPU | **AMD Radeon RX 590 GME，4 GiB VRAM**（驱动 31.0.21923.11000）。**注意：原文写"无 GPU"，本机有 GPU**，但以下事实须同时注意：无 `nvidia-smi`（非 NVIDIA），且 `torch`/CUDA 仍未安装 |
| 磁盘（各盘可用） | C: 23.8G、**D: 1.9G（本项目所在盘，非常紧张）**、E: 113.3G、F: 60.9G、G: 86.9G、H: 129.1G |
| 网络 | 需经本机 HTTP 代理 `127.0.0.1:3067`（直连超时；`git` 已全局配置该代理）。经代理后 PyPI 镜像与 GitHub 可达 |
| Python（系统） | **3.14.3**（`C:\Users\Administrator\AppData\Local\Programs\Python\Python314`） |
| Python（虚拟环境） | **3.13.13**，由 `uv` 创建于 `.venv/`（选 3.13 是为保证 wheel 可用性） |
| 已装包（.venv） | `numpy 2.5.3`、`Pillow 12.3.0`、`imagehash 4.3.2`、`scipy 1.18.1`、`pywavelets 1.10.0` |
| 未装 | `torch`、`onnxruntime`、`tokenizers`、`cv2` |
| ffmpeg | **8.1.2（BtbN win64 LGPL 静态构建）**，位于 `ffmpeg/bin/`，**不在 PATH**，由 `vfp.decode` 自动定位。构建号 `n8.1.2-53-g1005b294ff-20260915` |
| git | 2.53.0.windows.1，`user.name=CLoudXiao778` |
| uv | 0.11.18 |
| 包索引 | 清华 TUNA 镜像（`mirrors.tuna.tsinghua.edu.cn`，经代理可达） |

**该 ffmpeg 构建的已知能力边界【已实测】**：

- 可用：`libopenh264`(H.264)、`mpeg4`、`libvpx-vp9`、`libaom-av1`、`aac`、`libmp3lame`、`libopus`、
  `drawtext`、`zoompan`、`gblur`、`noise`、`testsrc2`、`gradients`、`smptebars`、
  `colorlevels`、`huesaturation`
- **不可用**：`libx264`、`libx265`（LGPL 构建未启用）、**`eq` 滤镜**（未编译）
- 没有名叫 `brightness` 的滤镜。做亮度调整须用 `colorlevels`（选项名为
  `rimin/gimin/bimin`，**不是** `rim/gim/bim`）+ `huesaturation`。
  写 `-vf brightness=...` 或 `-vf eq=...` 都会报 `Filter not found`。

#### 3.1.2 原文记录的交接机（Linux）【保留备查，非本机】

原文 §3.1 的数值属于**另一台机器**，在此完整保留以便对照，**不要**用它推断本机能力：

| 项 | 原值（Linux 交接机） |
|---|---|
| OS / Shell | Linux，POSIX shell |
| CPU | Intel Pentium G4560 @3.50GHz，2 核 4 线程 |
| 内存 | 31 GiB |
| GPU | 无 |
| 磁盘 | `/AstrBot/data` 59G，可用 51G |
| 网络 | 约 340 KB/s |
| Python | 3.12.13 |
| ffmpeg | 7.1.5-0+deb13u1 |

⚠️ **两台机器的 CPU 单核性能差距约一个数量级**（G4560 为 2 核 Skylake 世代，
i5-12400F 为 6 核 Alder Lake 世代）。原文所有【估算】类的吞吐数字都基于前者，
**在本机全部失效**。本机实测见 §3.5。

### 3.2 仓库路径

```
当前仓库根： D:\Coding\dsh\phash        ← 本机实际位置
远程仓库：   https://github.com/CLoudXiao778/phashVerify.git  (origin)
```

原文记录的 `/AstrBot/data/workspaces/project_b9f02e33-.../videofp` 与
`../research/` 一期素材**在本机不存在**（见 §0.2）。

### 3.3 git 状态【已实测】

本仓库由本次接手会话**新建**（`git init`）。原文 §3.3 记录的 4 个提交
（`66a2701`/`f5294d6`/`b24dac0`/`e9b2c1e`）属于另一台机器上的另一个仓库，
本机无法访问，**不要假设它们存在**。

### 3.4 文件清单与状态（本次接手新建）

| 文件 | 职责 | 状态 |
|---|---|---|
| `vfp/hashes.py` | pHash64 / dct256 / dHash64 / 镜像哈希 / 帧质量分 | 【已实测】phash64 与 imagehash 位级一致（4 张图差异 0 bit） |
| `vfp/decode.py` | ffmpeg 定位 / ffprobe 探测 / 容错解码（降级链） | 【已实测】正常文件；**降级路径可执行但当前损坏样本无法被它救回**，见 §14.2 |
| `vfp/normalize.py` | 黑边裁剪 / 方阵归一化 | 【已实测】合成 letterbox 用例精确恢复裁剪框 |
| `vfp/align.py` | 对角线投票对齐、片段定位、倍速精修、片段 IoU/F1 | 【已实测】17 项测试覆盖，含可辨识性反面用例 |
| `vfp/audiofp.py` | 谱图峰值地标指纹 + 多尺度变速 + 偏移投票 | 【已实测】与视频通道并联跑通 |
| `vfp/index.py` | Config / 建库 / 端到端匹配 / 融合打分 | 【已实测】3 参考片建库 + 18 条查询 |
| `vfp/eval.py` | 评测指标 + 离线阈值扫描 | 【已实测】与 `cli eval` 联调 |
| `vfp/cli.py` | `build` / `query` / `eval` 三个子命令 | 【已实测】三者均跑通 |
| `tools/make_corpus.py` | 生成合成语料 + 真值 + 自校验 | 【已实测】产出 3 源片 / 18 待检 / 6 损坏 |
| `tests/test_core.py` | 17 项正确性测试 | 【已实测】17/17 通过 |
| `demo.py` | 端到端演示（建库→查询→时间码证据） | 【已实测】跑通 |
| `ffmpeg/bin/` | 随仓库携带的 ffmpeg/ffprobe | 【已实测】126 MB 级二进制，已 gitignore |
| `README.md` / `CHANGELOG.md` | 原为 0 字节，本次已写入内容 | 【已实测】 |

### 3.5 本机吞吐【已实测 本次接手会话】

⚠️ **口径必须随数字一起引用**：本表测的是**本机 + 本实现**，
与原文 §5.7（Linux 交接机 + 原实现）**不可直接比较**。

| 环节 | 数值 | 口径 |
|---|---|---|
| pHash64（32×32 批量） | **14 417 帧/秒**（0.069 ms/帧） | 800 帧随机 32×32 uint8，含 scipy DCT |
| ffmpeg 解码 720p→320 宽灰度，2 fps | **0.057 s / 8 秒素材** | 单次进程，`-f rawvideo` 管道，含进程启动 |
| 建库（3 条参考片，含音频指纹） | **0.9 s**（约 3.3 帧/秒·条） | 每条 8 秒 / 16 帧；**含每条一次 ffmpeg 进程启动与音频解码** |
| 端到端查询 | **0.73 s / 条** | 18 条查询，含解码+归一化+3 参考片×2 镜像×5 倍速对齐+音频多尺度 |
| 全量评测（18 条，含音频与阈值扫描） | **13.1 s** | `cli eval --sweep` |

**结论：本机完全满足本项目的开发需求**，性能相对原文交接机有数量级余量
（原文建库/评测级别为"约 5 分钟 / 60 条"，本机同口径折算约 40 秒）。

### 3.6 磁盘注意事项【已实测】

本仓库所在 **D: 盘仅剩 1.9 GB**，非常紧张。本项目已按此约束设计：

- 解码走 `rawvideo` 管道，**不落盘 PNG**（原文 §4.1 的 PNG 落盘方式在本机不适用）
- 损坏样本的中间 TS 文件落在 `data/corpus/tmp/` 且用后即删
- `db/`、`queries/`、`ffmpeg/bin/`、`*.zip` 均已 gitignore
- 演示脚本工作目录 `demo_work/` 已 gitignore

若需扩大语料，请先确认磁盘余量（3 源片 + 18 待检 + 6 损坏约占用 12 MB）。

---

## 4. 数据资产（**两套管线的数字不可混用**）

### 4.1 管线 A：`research/`（一期，在仓库外）

- 位置：`../research/`（相对仓库根）
- 内容：32 秒合成基准片（4 张真实照片 + Ken-Burns 运镜 + 合成音轨），**28 个篡改变体**，1 秒抽帧得 32 帧
- 哈希实现：**第三方 `imagehash.phash`**，帧以 PNG（宽 320）落盘后计算
- 产出：`results.csv`、`results.json`、`audio_results.json`、`run.log`
- **口径**：32 帧样本、Hamming 阈值 ≤10、**无黑边归一化**（归一化是单独补做的对照实验）
- 主要脚本：`gen.py`、`analyze.py`、`audio.py`、`blackbar_fix.py`
- ⚠️ **此目录不在 git 仓库内**，未被版本管理

### 4.2 管线 B：`videofp/`（本仓库）

- 位置：`data/corpus/`
- 源片 4 条：`s1..s4.mp4`，各 20 秒 1280×720 30fps，每片=1 张真实照片（picsum）+ 缓慢推镜 + **各自不同的合成音轨**（避免音频侧无法区分源片）
- 负样本 6 条：`n1..n4`（lavfi 生成：testsrc2 / testsrc / gradients / smptebars）、`n5..n6`（照片 + 另一段音乐）
- 待检片：`queries/q001..q060.mp4`（54 正 + 6 负），**文件名中性化**
- 损坏样本：`queries/q900..q909.mp4`（10 条，基于 `q001` 的副本做文件级损坏）
- 真值：`groundtruth.csv`（60 行），`groundtruth_corrupt.csv`（10 行），`manifest_private.csv`（人类可读，**不参与评测**）
- **口径**：抽帧 2 fps（每条 40 帧）、Hamming 阈值 ≤10、**启用黑边归一化 + 镜像双查询**
- 与管线 A 的差异：抽样率不同（2fps vs 1fps）、归一化设置不同、哈希实现路径不同（自研 vs imagehash）
- ⚠️ **两套数字不可直接比较**

### 4.3 git 忽略情况（对交接极重要）

`.gitignore` 忽略了：`db/`、`results/*.json`、`data/corpus/queries/`、`data/corpus/tmp/`、`frames/`、`out/`

**后果**：新克隆的仓库**没有** 70 个待检视频、**没有** 指纹库、**没有** 评估结果。需要：
1. 重跑 `python3 tools/make_corpus.py`（会跳过已存在文件并重写 `groundtruth.csv`，约 5~8 分钟）
2. 重跑 `python3 -m vfp.cli build --sources "data/corpus/sources/*.mp4" --db db`
3. 重跑评估

磁盘上现存的 `db/` 是 15:33 建的，其 `meta.json` 的 config 含 **16 个字段**，而当前 `Config` 有 **17 个**（后加 `chroma_fallback`）。当前能正常加载（缺字段用默认值），但**若未来删除或改名字段，`Config(**meta['config'])` 会抛 `TypeError`**。建议加版本号字段并做兼容。

---

## 5. 已验证的事实【已实测】

### 5.1 正确性测试（`python3 -m tests.test_core`，6/6 通过）

| 测试 | 结果 |
|---|---|
| phash64 与 `imagehash.phash` 位级一致 | **差异 0 位（64/64）**，两张样例图各一次 |
| `hamming_matrix` | 构造向量验证通过 |
| `dct256` 结构 | 4×uint64 = 256 bit，自身距离 0 |
| 对齐：合成 3s 偏移 + 1.05 倍速 | 恢复 34 帧、覆盖率 1.0、**倍速估计 1.05** |
| 对齐：插广告切分 | 正确切成 2 段（conf 0.70 / 0.70） |
| 黑边裁剪 | (180,320) → (104,308) |

### 5.2 pHash 鲁棒性（管线 A 口径：32 帧、1fps、imagehash、无归一化）

命中帧数（同时间偏移下的帧级命中 / 32）：

| 变体 | 命中 | 变体 | 命中 |
|---|---|---|---|
| 换容器 mkv | 32/32 | 裁剪保留 85% | 19/32 |
| x265 CRF30 / VP9 / x264 CRF38 | 32/32 | 旋转 3° | 15/32 |
| 降采样 360p / 升采样 1440p | 32/32 | 不透明大水印（约 1/3 画面） | 11/32 |
| 平台二压（480p→720p） | 32/32 | 画中画遮挡（45%×50%） | 7/32 |
| 亮度/对比/饱和 | 32/32 | 屏摄近似（**温柔版**仿真） | 5/32 |
| 模糊 σ1.2 + 噪点 | 32/32 | **加黑边 letterbox** | **1/32** |
| 小水印 / 换音轨 / 音量 | 32/32 | **水平镜像** | **0/32** |
| 裁剪保留 95% | 31/32 | ↳ 黑边裁剪归一化后 | **32/32** |
| 字幕条 | 28/32 | ↳ 镜像双查询后 | **32/32** |
| 中段剪 3s（偏移投票） | 25/32 | 掐头去尾（偏移投票） | 20/32（偏移 −4s，与真实裁剪一致） |
| 中段插 5s 广告 | 18/32（裂成 2 段） | | |

**假阳性基线**：基准片 vs 无关视频，帧级 Hamming 最小值 **18**、均值 30.8、≤10 命中率 **0%**；4 张不同照片两两互比 = 30 / 32 / 32 / 36 / 36 / 40。
→ 结论：阈值 ≤10 安全；若放宽到 14~16 仍高于随机区。

**静帧补充实验**（4 张照片平均）：水平镜像 28~34、垂直 32、180° 30~36、90° 28~38（**全部等同随机**）；裁剪 5% 为 4~6、10% 为 6~14、20% 为 14~22；亮度 +8% 为 0。

### 5.3 音频指纹鲁棒性（管线 A 口径：简化 Shazam 星座图，3330 个哈希，20 秒合成音乐）

| 改动 | 严格命中率 | 容错 ±1 量化格 |
|---|---|---|
| 音量 −70% | 0.998 | 0.998 |
| 重采样 8 kHz | 0.850 | 0.850 |
| 窄带 300–3400 Hz | 0.796 | 0.823 |
| AAC 160k | 0.670 | 0.765 |
| MP3 64k | 0.670 | 0.761 |
| **变速 1.05×** | 0.608 | 0.755 |
| OPUS 48k | 0.620 | 0.722 |
| AAC 64k | 0.524 | 0.635 |
| 加混响 | 0.476 | 0.628 |
| **变速 1.25×** | 0.432 | 0.614 |
| 叠加 BGM | 0.486 | 0.538 |
| **变调 +4%** | **0.102** | 0.147 |
| **换配音（无关音轨）** | 0.016（≈噪声底） | 0.017 |

⚠️ **口径警告**：命中率**不是**召回率。真实系统靠"匹配哈希在时间偏移上汇聚成尖峰"判定，几十个哈希对齐即可能定罪。本表仅用于**横向比较**各种改动的破坏力。

### 5.4 chroma 通道判死实验【已实测，但脚本未落盘】

| 对比 | 最佳对角余弦 | 背景均值 | 对比度 |
|---|---|---|---|
| s1 自身 vs s1 | 1.000 | 0.891 | +0.109 |
| 无关音乐 vs s1 | 0.992 | 0.866 | **+0.126** |
| 无关音乐 vs s3 | 0.988 | 0.851 | +0.137 |
| s1 变调 +1 半音 vs s1 | 0.907 | 0.889 | +0.018 |

**结论**：无关内容的对比度（+0.126）**高于真配**（+0.109）→ 该通道无判别力 → 已默认关闭（`Config.chroma_fallback=False`）。
⚠️ **该实验是一次性内联脚本，未保存为文件**。若要写进论文，**必须重跑并落盘**（否则不可复现）。

### 5.5 首轮全量评估（`results/full.json`，2026-09-16 15:40）

```
threshold 0.2 | 54 正 / 6 负
recall 1.000 | false_positive_rate 0.667 | precision 0.931 | accuracy 0.933
source_top1_acc_on_positives 0.481 | mean_iou_localization 0.695 | n_undecodable 0
```

**⚠️ 这组数字包含已定位的 bug，禁止作为基线引用。**
根因（已定位，有逐条记录支撑）：chroma 通道对无关内容也给出 1.0 分；per-query 记录显示错误源是以 `v=0.000 / a=1.000 / method=chroma` 排到第一位的。所以 `FPR=0.667` 与 `source_top1=0.481` 都是该 bug 的产物。

### 5.6 chroma 关闭后的单点抽查【已实测·样本极小】

| 查询 | 真值 | 结果 |
|---|---|---|
| `q027`（s2 换容器 mkv） | s2 | 命中 s2：video 0.7952，音频 landmark 1.0，片段 ref 0.0–19.5s / query 0.0–19.5s，167 帧，mean_dist 2.05，speed 1.006 |
| `q005`（s1 强压 crf38） | s1 | 命中 s1：video 0.8787 |
| `q055`（无关负样本） | 无 | 正确拒绝：top_score 0.0486 |
| `q059`（无关负样本） | 无 | 正确拒绝：top_score 0.0844 |

⚠️ 2 正 2 负，**不构成统计结论**；且这 4 条是在 `_ensure_scaled` 懒加载补丁**之前**跑的。

### 5.7 本机吞吐【已实测】

| 环节 | 数值 |
|---|---|
| ffmpeg 解码+灰度缩放（720p 源）→ 1280×720 / 640×360 / 320×180 | 537 / 623 / 665 帧/秒 |
| videofp pHash 哈希 | **820 帧/秒** |
| videofp 单次解码 40 帧（含进程启动固定开销） | 0.81 秒（≈49 帧/秒） |
| 首轮全量评估（60 条 query，chroma 开启 + 音频多尺度急切计算） | 约 5 分钟 |

---

## 6. 未完成 / 未验证清单（**严禁当作已完成**）

| 编号 | 事项 | 现状 | 影响 |
|---|---|---|---|
| **V1** | **chroma 关闭后的全量重评** | **从未运行** | 当前没有可用的性能基线；`results/full.json` 是坏数据 |
| **V2** | 消融实验（关黑边归一化 / 关镜像 / 关音频 / 换哈希） | `run_experiments.sh` 从未执行 | 无法论证各组件的贡献 |
| **V3** | 损坏样本评估（q900–q909） | 样本已生成，`results/corrupt.json` **不存在** | 容错解码与降级报告的鲁棒性未验证 |
| **V4** | `tools/bench.py` | 从未运行 | 无规模/性能数据 |
| **V5** | `_ensure_scaled` 音频懒加载补丁 | 语法通过，**从未执行** | 可能存在运行时错误，会直接影响所有含音频的查询 |
| **V6** | `tools/make_phash_corpus.py`（1561 行） | **从未运行**（`data/phash` 不存在） | 见 §11「待定」 |
| **V7** | `dct256` / `dhash64` 的检索评测 | 仅验证了 `dct256` 的结构，**未做任何检索指标** | 哈希选型无数据支撑 |
| **V8** | 语义检索层 | **零代码**，仅 3 份理论文档 | 增值目标尚未开始 |
| **V9** | `tools/analyze_results.py`、`run_all.sh` | 从未运行 | 阈值扫描与批跑流程未验证 |
| **V10** | 真实视频语料 | 用户承诺提供，**尚未提供** | 当前全部结论基于合成语料 |

---

## 7. 待办任务（按优先级，含验收标准）

### P0 — 建立可信基线（必须先做）

1. **验证 `_ensure_scaled`（V5）后再重跑全量评估**（V1）
   - 命令：`python3 -m vfp.cli eval --db db --gt data/corpus/groundtruth.csv --report results/full_v2.json`
   - 验收：产出 `results/full_v2.json`；报告 recall / FPR / precision / source_top1 / mean_IoU；**与 5.5 节旧数据并列展示，并说明旧数据作废原因**
   - 预期（**预期，非事实**）：FPR 应从 0.667 显著下降，`source_top1` 应上升。**必须用实测取代预期。**

2. **损坏样本评估**（V3）
   - 命令：`python3 -m vfp.cli eval --db db --gt data/corpus/groundtruth_corrupt.csv --report results/corrupt.json`
   - 验收：逐条报告 `usable_ratio`、`recovered`、`decode_note`，明确哪些损坏模式导致 `undecodable`，并验证"优雅降级"确实生效

### P1 — 补论证

3. **消融实验**（V2）：逐个关闭黑边归一化 / 镜像双查询 / 音频通道，产出对比表
4. **哈希选型对比**（V7）：`phash64` vs `dct256` vs `dhash64` 在同一语料上的 recall / FPR / 耗时
5. **接入真实视频语料**（V10）：按 §8.3 的命名与真值规范

### P2 — 增值目标

6. **语义检索最小验证**（V8），仅做三件事，禁止扩大范围：
   - E1：文本→图像检索 R@1 / R@5（小样本，验证本地小模型可用）
   - E2：语义余弦矩阵与 pHash 距离矩阵的 Spearman 相关系数（验证"判据正交"）
   - E3：两级架构收益（语义召回 N→100 后指纹精判 vs 全库两两比对）
   - 约束：**本机无 GPU**，须走量化小模型或申请云租；禁止下载 >100MB 权重（C5）

### P3 — 工程完善

7. 跑通 `bench.py` / `analyze_results.py` / `run_all.sh`，确保批跑流程可用
8. `db/meta.json` 加 schema 版本号，`load_db` 做字段兼容（见 §4.3）

---

## 8. 评测协议（防自欺，后续 Agent 必须遵守）

### 8.1 `eval` 输出的指标定义

| 指标 | 定义 |
|---|---|
| `recall` | 正样本中被判命中（`top_score ≥ match_threshold`）的比例 |
| `false_positive_rate` | 负样本中被判命中的比例 |
| `precision` | TP / (TP + FP) |
| `source_top1_acc_on_positives` | 正样本中"最高分候选 == 真值源片"的比例 |
| `mean_iou_localization` | 仅在有 `gt_segment` 的行上计算：预测片段（参考时间轴）与真值区间的 IoU 均值 |
| `usable_ratio` | 解码可用帧数 / 总抽帧数（衡量损坏视频的降级程度） |

### 8.2 阈值扫描的正确做法

`eval` 只用单一 `match_threshold` 跑一次，但会把每条 query 的 `top_score` 写入报告。
**阈值扫描须由 `tools/analyze_results.py` 离线用已存分数重算**（召回 vs 假阳率曲线），**不要**为每个阈值重跑一遍匹配（浪费且可能引入不一致）。

### 8.3 真实视频接入规范（用户已同意）

1. **文件名中性化**：`queries/qNNN.mp4`；真实含义只写 `groundtruth.csv`
2. **必须同时给元数据两组**：A 组保留原始元数据（可测转码链溯源），B 组 `-map_metadata -1` 清洗（测纯视觉能力）
3. **必须包含负样本**（与所有参考片无关的片子），否则假阳率不可计算
4. **损坏样本须标注类型**（翻 bit / 翻 byte / 破坏头 / 截断），因为不同位置后果差异极大
5. `groundtruth.csv` 列：`qid, file, source, transform, verdict, gt_segment, corrupt`

---

## 9. 技术选型与已论证的取舍

| 决策 | 理由（有实测支撑） |
|---|---|
| 默认 `phash64` | 与社区实现位级一致，便于对照与审稿解释 |
| **必须做黑边检测裁剪** | 加黑边使命中掉到 1/32；裁剪归一化后回到 32/32 |
| **必须做镜像双查询** | 镜像使命中掉到 0/32；翻转哈希参与比对后回到 32/32 |
| **必须有序列对齐** | 时序类攻击帧级存活 100%，但朴素同轴比对崩到 50%（均值 Hamming 28.3） |
| **必须做片段定位** | 插广告会形成两条平行线，必须按段输出时间码 |
| chroma 通道默认关闭 | 无关内容对比度高于真配，无判别力（§5.4） |
| 音频指纹做多尺度变速回放 | 变速 1.05× 使严格命中率掉到 0.608 |
| 语义层只做召回 | 语义相似度无法给出"同一性"结论（§1.2/§1.3） |

---

## 10. 环境与操作坑（踩过的，别再踩）

1. **`pkill -f "某字符串"` 会把执行它的 shell 自己杀掉**（若该字符串出现在自身命令行中）。本次已两次自伤。改用 `os.kill(pid, SIGTERM)` 或先 `pgrep` 取 PID。
2. **`rm -rf` 被安全策略拦截**。删除目录请用 Python `shutil.rmtree(..., ignore_errors=True)`。
3. **ffmpeg `drawtext` 含中文/空格时引号易被 shell 吃掉**，导致 `Error parsing filterchain`。本次 `o_subtitle.mp4` 就是这样失败的。建议把 filtergraph 写入脚本文件或用 `-filter_complex_script`。
4. **生成脚本必须检查返回值**。本次 `make_corpus.py` 曾因变换函数只 `return` 命令字符串而**没有真正执行 ffmpeg**，却把 60 行真值写进了 `groundtruth.csv`（54 个文件根本不存在）。修复：`ok = run(fn(...))`。**→ 任何批量生成脚本，结束后必须做"文件数与时长校验"。**
5. **`loaddb` 与 `Config` 字段强耦合**（§4.3）。
6. **`life` / `mandelbrot` 等 lavfi 滤镜极慢**（20 秒 720p 视频耗 CPU 3.5 分钟以上）。生成负样本请用 `testsrc2` / `testsrc` / `gradients` / `smptebars`。
7. **`data/corpus/queries/` 与 `db/` 被 gitignore**，重装环境后必须重新生成。

---

## 11. 待定/存疑（**禁止脑补，需要时先做实验**）

1. **`tools/make_phash_corpus.py` 的真实状态**
   - 已知：1561 行，已被提交（`e9b2c1e`），docstring 声称产出 `ref/ variants/ neg/ work/ meta/` 到 `data/phash`，CLI 默认 `--refs 2 --duration 20 --jobs 4 --seed 7`，支持 `--groups temporal|geometry|rescale|overlay|audio|container|codec|combo|corrupt`
   - 已核实：**`data/phash` 目录不存在**
   - **未核实**：该脚本是否曾经成功运行过、其 1561 行实现是否正确、是否可用。**我没有验证过它的任何一行逻辑。**
   - 它与 `tools/make_corpus.py` 是**两套独立设计**（前者更完整），二者的输出格式与真值 schema 不同。**接手前必须先决定以哪一套为准**，并在文档中统一口径。
2. **语义检索层的真实性能**：全部为【估算】。本机 CPU 上 CLIP ViT-B/32 的吞吐（约 1 图/秒）是推算，未实测。
3. **显卡吞吐估算**：V100 / 2080Ti / B580 的吞吐均为【估算】，来自硬件倍率推算，**未在任何真实卡上实测**。
4. **"chroma 修复后性能会变好"是预期不是事实**（V1 未跑）。
5. **`dct256` 是否比 `phash64` 更抗篡改**：无数据（V7）。
6. 一期 `research/` 目录未纳入版本管理，其脚本能否在当前环境完整复现，未验证。

---

## 12. 参考文档索引

**仓库内（`videofp/docs/`）**
- `语义检索验证报告.md` —— 端侧双塔 vs VLM 转述、视频侧实现、语义与指纹的关系、三层架构建议
- `语义检索硬件要求评估.md` —— 成本构成、模型选型、规模-硬件对照、分档建议、五条工程铁律
- `视频标签实现原理.md` —— 四代标签做法、大模型介入的四层、视频标签的时间轴形态、音频事件信号

**仓库外（`../research/`，一期实测）**
- `results.csv` / `results.json` —— 28 个变体的 pHash 鲁棒性（§5.2 数据源）
- `audio_results.json` —— 音频改动对照（§5.3 数据源）
- `gen.py` / `analyze.py` / `audio.py` / `blackbar_fix.py`

**外部资料（引用来源见各文档末尾）**
- Facebook ThreatExchange（PDQ / TMK+PDQF）、VCDB / VCSL 数据集、Steinebach 裁剪旋转鲁棒哈希、CLIP-as-service 官方基准、OpenSearch HNSW 内存公式、QVHighlights / Moment-DETR、AudioSet / CLAP、MobileCLIP

---

## 13. 交接人给接手 Agent 的三条提醒

1. **优先级顺序不能乱**：先把 P0（可信基线 + 损坏评估）做完，再碰语义检索。**没有可信基线的项目，后面所有实验都白做。**
2. **不要相信任何没有落盘脚本的数字**。本文件已如实标注哪些实验的脚本未保存（§5.4）。凡是要写进论文的数字，都必须有对应的、可重跑的脚本文件。
3. **本文件里凡是标了【未验证】【估算】【待定】的地方，都是我不知道的，不是我认为没问题的。** 请以实验数据为准，不要以文档措辞的肯定语气推断完成度。

---

## 14. 本次接手会话的实测记录（对照原文声明）

本节是**接手 Agent 新增**的。全部数字来自本机实际执行，可用仓库内脚本重跑
（命令写在每一行后面）。**口径**：合成语料（3 参考片 / 18 待检 / 6 损坏），
抽帧 2 fps、Hamming 阈值 ≤10、启用黑边裁剪 + 镜像双查询 + 音频通道。

### 14.1 复现原文结论的项【已实测】

| 原文声明 | 本次实测 | 重跑命令 |
|---|---|---|
| §5.1 phash64 与 imagehash 位级一致 | **4 张图差异 0 bit** | `python -m tests.test_core` |
| §5.1 对齐可恢复 1.05 倍速 | **1.0520**（100 帧查询，误差 0.002） | 同上 |
| §5.1 插广告应切成 2 段 | **切成 2 段**（q 2.0–3.0 / 6.5–18.5） | 同上 |
| §5.1 黑边裁剪 (180,320) | **精确裁掉 (38,38,6,6)** | 同上 |
| §9 加黑边会毁掉 pHash，裁剪归一化能救回 | **未归一化距离 24 → 归一化后 0** | 同上 |
| §9 镜像必须做双查询 | **直接比对命中 0/16 → 镜像哈希后命中 16/16** | 同上 |
| §5.2 假阳基线：无关内容最小 Hamming 远大于阈值 | **最小 26、均值 31.3、≤10 命中率 0.00** | 同上 |
| §5.2 亮度 +8% 对 pHash 基本无影响 | **距离 2 bit** | 同上 |

### 14.2 P0 可信基线（原文 §7 P0 一直未完成）【已实测】

**全量评测**（18 条查询，`results/full.json`）：

```
threshold 0.2 | 15 正 / 3 负
recall 1.000 | false_positive_rate 0.000 | precision 1.000 | accuracy 1.000
source_top1_acc_on_positives 1.000
mean_iou_localization 0.899（仅 2 条有 gt_segment）
segment_precision/recall/f1 @τ=0.5 = 1.000 / 1.000 / 1.000
n_undecodable 0 | mean_usable_ratio 1.000
```

⚠️ **口径警告（必读）**：
1. 这组数字**远好于**原文 §5.5 的坏数据（recall 1.0 / FPR 0.667），但
   **不能**因此宣称"修好了 chroma 就变好"——样本只有 3 个负样本，
   **FPR=0 的分辨率上限是 1/3**，不构成统计结论（C8）。
2. 语料是**合成**的（推镜 + 合成音轨），**不是真实影视内容**。原文 §6 V10
   （真实语料）仍未解决，本组数字**不可**写进论文当最终结果。
3. `mean_iou_localization` 只覆盖 2 条，因为只有 `trim_head` / `trim_both`
   两个变体带真值片段。**这个 n 太小，不能当定位性能**。

**损坏样本评测**（6 条，`results/corrupt.json`）：

| 损坏类型 | 结果 | 说明 |
|---|---|---|
| `flip_byte`（翻 1 字节） | **命中** 0.6159 | 翻转点在 mdat 载荷中段，影响可忽略 |
| `flip_bits`（翻 48 bit） | **命中** 0.2450 | 仅轻微降级 |
| `zero_middle`（抹 8 KB） | **命中** 0.3599 | 优雅降级生效 |
| `break_header` | **未命中** | `moov atom not found`，解复用阶段即失败 |
| `truncate_10`（截 5%） | **未命中** | 同上 |
| `truncate_30`（截 15%） | **未命中** | 同上 |

**关于降级链的诚实结论**：`n_recovered_by_fallback = 0`。本次**逐一验证**了
三种失败样本，ffmpeg 报错均为 `moov atom not found`——**容器结构已损毁，
任何解码级容错参数都无法恢复**（已验证加 `+discardcorrupt+genpts` 与
`err_detect ignore_err` 同样失败）。因此：

- 降级链（重封装 MPEG-TS 后再解码）**代码可达且会被执行**，但对**本次语料
  的损坏模式无法救回**；不要把它当作"能修复损坏视频"的能力宣称。
- 原文 §6 V3「容错解码鲁棒性未验证」→ 本次**已验证**，结论是
  **能优雅降级（不崩溃、如实报告），但无法修复结构性损坏**。

### 14.3 原文技术描述中被证伪/需修正的点【已实测】

| # | 原文说法 | 实测发现 | 新实现的做法 |
|---|---|---|---|
| 1 | §9「翻转哈希参与比对」（未说实现方式） | **哈希位重排不可能实现镜像**。实测置换后距离 **34 bit ≈ 随机 32 bit**。根因：镜像使 DCT 奇数列系数**翻转符号**，位序与"是否大于中位数"的判定同时改变 | 改为**像素域翻转帧后重算哈希**（`phash64_mirrored`），实测命中 16/16 |
| 2 | §5.1 记录倍速可恢复（默认已解决） | **短查询下倍速不可辨识**。12 帧 / 6 秒查询上，网格候选 1.0 与 1.05 命中数**并列**；精修把倍速拉到 **0.8457**，导致 6 秒素材的参考片段被算成 **5 秒** —— 时间码证据是错的 | 增加**可辨识性判据**：候选倍速命中数并列时不采纳精修，退回网格值，并在结果里输出 `speed_identifiable` 标志 |
| 3 | §2 C6「不用 scipy」 | 自研正交 DCT 与 imagehash 用的 `scipy.fftpack.dct`（未归一化）**不是同一尺度**，导致 4 张图共 **112 bit** 不一致（中位数阈值对整体缩放不敏感，但系数间相对大小会变） | 改用 scipy DCT，差异降为 **0 bit**；scipy 本就是 imagehash 传递依赖 |
| 4 | §10.3 暗示亮度类滤镜可用 | 本机 ffmpeg 构建**没有 `eq` 滤镜**，也**没有 `brightness` 滤镜**；`colorlevels` 的选项名是 `rimin` 而非 `rim` | 语料生成改用 `colorlevels=rimin=...` + `huesaturation` |
| 5 | §4.3 建议 `db/meta.json` 加版本号 | 已实现 | `SCHEMA_VERSION=2` + `Config.from_dict` 宽容加载（未知字段忽略、缺失字段用默认），不再抛 `TypeError` |
| 6 | §8.2 要求阈值扫描离线重算 | 已实现 | `cli eval --sweep` 用已存 `top_score` 离线重算，不重跑匹配 |

### 14.4 仍然未知 / 未做（**不要当作已完成**）

| # | 事项 | 状态 |
|---|---|---|
| U1 | **真实影视语料**（原文 V10） | **仍未提供**。当前全部结论基于合成语料 |
| U2 | 消融实验（关黑边 / 关镜像 / 关音频 / 关倍速搜索） | 代码已支持对应开关（`--no-crop-black` 等），**但未跑对比表** |
| U3 | 哈希选型对比（phash64 vs dct256 vs dhash64） | 三者都已实现，**未做检索指标对比** |
| U4 | `chroma` 通道 | **本实现未包含**（原文 §5.4 已判死）。原文结论未在本机重跑 |
| U5 | 语义检索层（原文 V8） | **零实现**，仍是增值目标 |
| U6 | 规模化性能（数百/数千参考片） | 未测。当前为线性扫描，参考片规模上去需要 ANN |
| U7 | 原文 §5.2/§5.3 的具体数值（32/32 命中表、音频改动表） | **本机未复现**（对应语料与脚本不在本机），不要引用为本机实测 |
| U8 | 原文 §5.4 chroma 判死实验 | 原实验脚本未落盘，本机无法复核 |

### 14.5 复现全部结果的命令

```powershell
# 0) 依赖（uv 管理虚拟环境）
uv venv --python 3.13 .venv
uv pip install --python .venv\Scripts\python.exe numpy pillow imagehash scipy

# 1) 正确性测试（17 项）
.venv\Scripts\python.exe -m tests.test_core

# 2) 生成合成语料（含文件数/时长自校验）
.venv\Scripts\python.exe -m tools.make_corpus --out data/corpus --refs 3 --negs 3 --variants-per-ref 5

# 3) 建库
.venv\Scripts\python.exe -m vfp.cli build --sources "data/corpus/sources/*.mp4" --db db

# 4) 全量评测（含离线阈值扫描）
.venv\Scripts\python.exe -m vfp.cli eval --db db --gt data/corpus/groundtruth.csv --report results/full.json --sweep

# 5) 损坏样本评测
.venv\Scripts\python.exe -m vfp.cli eval --db db --gt data/corpus/groundtruth_corrupt.csv --report results/corrupt.json

# 6) 端到端演示（自建语料，含镜像/黑边/损坏/负样本四类对照）
.venv\Scripts\python.exe demo.py

# 7) 单条查询（打印时间码证据）
.venv\Scripts\python.exe -m vfp.cli query --db db --query data/corpus/queries/q001.mp4
```
